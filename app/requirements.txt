# Was 3.1.0 — fixes CVE-2026-27205, CVE-2025-47278
flask==3.1.3

# Was 5.0.0 — fixes CVE-2024-6866, CVE-2024-6844, CVE-2024-6839
flask-cors==6.0.0

# Was 2.32.3 — fixes CVE-2024-47081, CVE-2026-25645
requests==2.33.0

gunicorn==23.0.0

# Optional: Docker SDK. Used to docker-stop / docker-start the *arr container
# directly (when /var/run/docker.sock is mounted), which is more reliable than
# the *arr shutdown API on hosts with a container restart policy.
docker==7.1.0

# Background scheduler for /api/schedules — fires repair jobs on a cron.
APScheduler==3.10.4

# Notifications: Apprise drives 100+ services (Discord/Telegram/ntfy/…).
# Signal goes direct to signal-cli-rest-api via requests (see app/notify.py).
apprise==1.9.2
